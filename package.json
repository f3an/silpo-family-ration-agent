{
  "name": "silpo-family-ration-agent",
  "version": "0.1.0",
  "private": true,
  "description": "Monorepo root — see api/ and client/. Only owns git hooks (husky must run from the repo root).",
  "scripts": {
    "prepare": "husky"
  },
  "devDependencies": {
    "husky": "^9.1.7"
  }
}
